{% extends 'base.html.twig' %}

{% block title %}Achievements{% endblock %}

{% block body %}
    <h1>Achievements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ achievement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ achievement.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ achievement.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ achievement.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_achievements_index') }}">back to list</a>

    <a href="{{ path('app_achievements_edit', {'id': achievement.id}) }}">edit</a>

    {{ include('achievements/_delete_form.html.twig') }}
{% endblock %}
