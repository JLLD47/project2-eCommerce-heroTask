{% extends 'base.html.twig' %}logout

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

    <button onclick="window.location.href='{{ path('app_login') }}'">Login</button>
    <button onclick="window.location.href='{{ path('app_register') }}'">Sign In</button>

{% endblock %}
