{% extends 'base.html.twig' %}

{% block body %}
    <div class="grid grid-cols-3 gap-4 h-[calc(100vh-124px)] ">
        <!-- Sección Izquierda: Información del Usuario -->
        <div class="rounded-lg">
            <div class="p-4 bg-tyrian-purple text-white">
                <div class="mb-4 font-bold flex items-center justify-center font-H text-2xl">{{ user.username }} </div>
                <div class="mb-4">
                    {% if app.user.profession == 'Warrior' %}
                        <img id="loginImage"
                             src="{{ asset('images/warriorCHAR.png') }}"
                             alt="Warrior Image" class="h-80 mx-auto max-w-full"/>
                    {% elseif app.user.profession == 'Mage' %}
                        <img id="loginImage"
                             src="{{ asset('images/mageCHAR.png') }}"
                             alt="Mage Image" class="h-80 mx-auto max-w-full"/>
                    {% elseif app.user.profession == 'Rogue' %}
                        <img id="loginImage"
                             src="{{ asset('images/rogueCHAR.png') }}"
                             alt="Rogue Image" class="h-80 mx-auto max-w-full"/>
                    {% endif %}
                </div>
                <div class="space-y-2">
                    <span class="hidden">{{ user.CurrentXp }}</span>
                    <img src="" id="levelUp" alt="">
                    <div>LEVEL: {{ user.CurrentLevel }}</div>

                    <div>
                        STRENGTH: {{ user.strength }}
                        <span id="strength-required" class="hidden">{{ user.StrXpRq }}</span>
                        <span id="strength-current" class="hidden">{{ user.StrCurrent }}</span>
                        <div class="progress-bar h-8 bg-gray-300  rounded-full">
                            <div class="progress h-8 bg-blue-600  rounded-full" id="strength-progress"></div>
                        </div>
                    </div>
                    <div>
                        INTELLIGENCE: {{ user.intelligence }}
                        <span id="intelligence-required" class="hidden">{{ user.IntXpRq }}</span>
                        <span id="intelligence-current" class="hidden">{{ user.IntCurrent }}</span>
                        <div class="progress-bar h-8 bg-gray-300  rounded-full">
                            <div class="progress h-8 bg-blue-600  rounded-full" id="intelligence-progress"></div>
                        </div>
                    </div>
                    <div>
                        CONSTITUTION: {{ user.constitution }}
                        <span id="constitution-required" class="hidden"> {{ user.ConstXpRq }}</span>
                        <span id="constitution-current" class="hidden">{{ user.ConstCurrent }}</span>
                        <div class="progress-bar h-8 bg-gray-300 rounded-full">
                            <div class="progress h-8 bg-blue-600  rounded-full" id="constitution-progress"></div>
                        </div>
                    </div>
                    <div>
                        CHARISMA: {{ user.charisma }}
                        <span id="charisma-required" class="hidden">{{ user.ChaXpRq }}</span>
                        <span id="charisma-current" class="hidden">{{ user.ChaCurrent }}</span>
                        <div class="progress-bar h-8 bg-gray-300 rounded-full">
                            <div class="progress h-8 bg-blue-600  rounded-full" id="charisma-progress"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Sección Central: Tareas Incompletas -->
        <div class="p-4 bg-moonstone text-white h-[calc(100vh-124px)] rounded-lg overflow-y-scroll">
            <div class="text-center mb-8">
                <button class="bg-orangePeel mb-8 text-black  hover:bg-tyrianPurple hover:text-white font-bold py-2 px-6 rounded"
                        onclick="window.location.href='{{ path('app_task_new') }}'">
                    Add a New Quest!
                </button>
                <div class="flex items-center justify-center mb-4">
                    <img src="{{ asset('/images/QQuest.png') }}" class="h-11 mr-2"/>
                    <h2 class="text-lg font-bold">Quests</h2>
                </div>
            </div>
            <div class="space-y-4">
                {% for task in userTasks %}
                    {% if not task.completed and not task.failed  %}
                        <div class="mb-6 p-4 bg-white rounded-lg shadow">
                            <!-- Tipo de tarea -->
                            <p class="stat hidden text-center text-gray-700 mt-2">{{ task.type }}</p>
                            <img src="" class="statImage h-14 mx-auto">
                            <!-- Título de la tarea con enlace -->
                            <a href="{{ path('app_task_edit', {'id': task.id}) }}"
                               class="block text-center font-bold text-blue-950 text-xl">
                                {{ task.title }}
                            </a>

                            <!-- Descripción de la tarea -->
                            <p class="text-center text-gray-700 mt-2">{{ task.description }}</p>

                            <!-- Botones de acción -->
                            <div class="flex justify-center space-x-4 mt-4">
                                <!-- Botón: Completar -->
                                <form action="{{ path('task_done', {'id': task.id}) }}" method="POST">
                                    <input type="hidden" name="_method" value="PATCH">
                                    <button type="submit"
                                            class="bg-green-500 hover:bg-green-600 text-black font-bold py-2 px-4 rounded w-32">
                                        Complete!
                                    </button>
                                </form>

                                <!-- Botón: Abandonar -->
                                <form action="{{ path('app_task_delete', {'id': task.id}) }}" method="POST">
                                    <button type="submit"
                                            class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded w-32">
                                        Forfeit
                                    </button>
                                </form>

                                <!-- Botón: Editar -->
                                <form action="{{ path('app_task_edit', {'id': task.id}) }}" method="POST">
                                    <input type="hidden" name="_method" value="PATCH">
                                    <button type="submit"
                                            class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded w-32">
                                        Edit
                                    </button>
                                </form>
                            </div>
                        </div>
                    {% endif %}
                {% endfor %}
            </div>
        </div>

        <!-- Sección Derecha: Tareas Completadas y Fallidas -->
        <div class="p-4 bg-tea-green">
            <div class="grid grid-rows-2 gap-4 h-full">
                <!-- Bloque Superior: Completadas -->
                <div class="bg-green-300 p-4 rounded">
                    <div class="flex items-center justify-center mb-4">
                        <img src="{{ asset('/images/CQuest.png') }}" class="h-11 mr-2"/>
                        <h2 class="text-lg text-white font-bold">Completed</h2>
                    </div>
                    <ul class="space-y-2">
                        {% for task in userTasks|reverse|slice(0,5) %}
                            {% if task.completed %}
                                <li class="p-2 bg-white rounded shadow">
                                    {{ task.title }}
                                    <a href="{{ path('app_task_redo', {id: task.id}) }}">
                                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"
                                             class="size-6">
                                            <path fill-rule="evenodd"
                                                  d="M4.755 10.059a7.5 7.5 0 0 1 12.548-3.364l1.903 1.903h-3.183a.75.75 0 1 0 0 1.5h4.992a.75.75 0 0 0 .75-.75V4.356a.75.75 0 0 0-1.5 0v3.18l-1.9-1.9A9 9 0 0 0 3.306 9.67a.75.75 0 1 0 1.45.388Zm15.408 3.352a.75.75 0 0 0-.919.53 7.5 7.5 0 0 1-12.548 3.364l-1.902-1.903h3.183a.75.75 0 0 0 0-1.5H2.984a.75.75 0 0 0-.75.75v4.992a.75.75 0 0 0 1.5 0v-3.18l1.9 1.9a9 9 0 0 0 15.059-4.035.75.75 0 0 0-.53-.918Z"
                                                  clip-rule="evenodd"/>
                                        </svg>
                                    </a>
                                </li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </div>

                <!-- Bloque Inferior: Fallidas -->
                <div class="bg-red-300 p-4 rounded">
                    <div class="flex items-center justify-center mb-4">
                        <img src="{{ asset('/images/FQuest.png') }}" class="h-11 mr-2"/>
                        <h2 class="text-lg text-white font-bold">Failed</h2>
                    </div>
                    <ul class="space-y-2">
                        {% for task in userTasks|reverse|slice(0,5) %}
                            {% if task.failed %}
                                <li class="p-2 bg-white rounded shadow">
                                    {{ task.title }}
                                </li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/stats.js') }}"></script>
{% endblock %}
